import styled from 'styled-components';

const FooterWrappter = styled.footer`
  padding: 30px;
  background-color: darkcyan;
`;

function Footer() {
  return (
    <FooterWrappter />
  );
}

export default Footer;
