import INIT_MAIN_MENU_ITEMS_STATE from '../status/mainMenuItems';

const mainMenuItemsReducer = (state = INIT_MAIN_MENU_ITEMS_STATE, action) => {
  switch( action.type ) {
    default:
      return state;
  }
};

export default mainMenuItemsReducer;
