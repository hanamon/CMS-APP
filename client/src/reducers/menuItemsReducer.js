import INIT_MENU_ITEMS_STATE from '../status/menuItems';

const menuItemsReducer = (state = INIT_MENU_ITEMS_STATE, action) => {
  switch( action.type ) {
    default:
      return state;
  }
};

export default menuItemsReducer;
